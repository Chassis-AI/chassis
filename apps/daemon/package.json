{
  "name": "@chassis/daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Daemon CHASSIS — ingestion → boucle harness → persistance. Le chaînon entre le métier et le cockpit.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsx src/main.ts",
    "dev": "tsx src/main.ts",
    "test": "node --test --import tsx ./src/__tests__/*.test.ts"
  },
  "dependencies": {
    "@chassis/core": "workspace:*",
    "@chassis/providers": "workspace:*",
    "@supabase/supabase-js": "^2.45.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.42",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
